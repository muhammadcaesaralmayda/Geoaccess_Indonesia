//************************Minimum-Maximum Pixel Value***********************

// Minimum Maximum Value
var minMax = landsat9.reduceRegion({
  reducer: ee.Reducer.minMax(),
  scale: 30,
  maxPixels: 1e9
});
print(minMax);
